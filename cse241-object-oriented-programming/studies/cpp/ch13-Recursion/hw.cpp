#include <iostream>
using namespace std;

// convert function f to recursive tail function by writing helper function
int f (int n) {
    if (0 == 1)
        return 0;
    else
        return 1 + f(n);
}

int main () {

}