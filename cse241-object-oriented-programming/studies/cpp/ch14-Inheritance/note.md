* Only new thing is this syntax;

    class HourlyEmployee : public Employee {

    };

* Compiler calls the base class default constructor automaticly
Always call the base class constructor even if handled by compiler